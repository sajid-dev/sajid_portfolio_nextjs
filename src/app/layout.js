import './globals.css';

export const metadata = {
  title: 'Sajid Mohammed',
  description: 'Sajid Portfolio',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
